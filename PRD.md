# 2026 Mid-year Workshop 汇报管理平台 — 功能描述（PRD）

> 版本：v0.1（草稿，待确认）
> 日期：2026-08-05

## 1. 背景与目标

2026 Mid-year Workshop 上，每位同事需要汇报自己的 high impact 项目。平台支撑完整的"汇报前准备 → 汇报当天演讲 → 汇报后反馈"闭环：

- **汇报前**：上传 PPT，AI 按统一评审维度给出修改意见，支持迭代修改
- **汇报当天**：每人上台时用浏览器录音，AI 转写演讲内容
- **汇报后**：AI 结合 PPT 内容、演讲转写、历史汇报记录，给出评论与反馈
- 所有历史记录可留存，AI 后续点评时可参考个人历史

## 2. 用户与规模

- 团队同事约 **20 人**，每人汇报 **10 分钟**
- 角色：**管理员**（建号、配置评审维度、查看全局进度）与 **普通用户**（上传、录音、查看自己的反馈）
- 登录：账号密码，由管理员预先创建账号
- 使用语言：中文界面

## 3. 核心功能

### 3.1 登录与账号

- 管理员创建账号（姓名、邮箱/用户名、初始密码）
- 用户登录后可修改密码
- 管理员可重置密码、停用账号

### 3.2 汇报前：PPT 上传与 AI 修改意见

- 用户上传 PPT（.pptx / .pdf），系统提取内容（文字、备注，尽量含图片中的文字 OCR）
- AI 按固定评审维度逐项给出修改意见，输出为可勾选的"意见清单"
- 用户按意见修改后可重新上传，AI 对比新旧版本给出增量意见
- 管理员可配置/编辑评审维度说明，评审维度默认如下：

#### 评审维度（high impact 项目）

1. **业务影响力**：项目对业务的价值与影响范围
2. **创新性**：思路/方案的创新点
3. **落地情况与完成度**：当前进展、完成百分比、里程碑达成
4. **遇到的问题与解决方案**：过程中遇到的关键问题、如何解决
5. **数据支撑**：关键数据、指标、前后对比
6. **下一步计划**：后续安排与可落地性
7. **表达与结构**（辅助维度）：逻辑清晰度、重点突出程度

### 3.3 汇报当天：演讲录音与转写

- 汇报人在自己账号下选择本次汇报，点击"开始录音"
- 浏览器麦克风录音（MediaRecorder），可暂停/继续/停止
- 停止后上传音频，系统异步转写为文字
- 管理员可查看全局"汇报进度"（谁已上传 PPT、谁已录音、谁已获得反馈）

### 3.4 汇报后：AI 评论与反馈

- AI 综合以下材料生成反馈：
  - 最终版 PPT 内容
  - 演讲转写文本
  - 该用户的历史汇报记录与历史反馈（记忆）
- 反馈包含：整体评价、亮点、可改进点、与历史汇报的对比/进步
- 反馈仅本人与管理员可见

### 3.5 记忆存储

- 每个人的历史汇报（PPT、录音、转写、反馈）永久留存
- AI 点评时自动带入个人历史摘要，实现连续性反馈

## 4. 技术方案（建议）

- **前端/后端**：Next.js（App Router）+ TypeScript
- **数据库**：SQLite（Prisma ORM）——20 人规模单机完全够用，易备份
- **文件存储**：服务器本地磁盘（PPT 与音频），定期备份
- **认证**：session 登录（账号密码 + 密码哈希）
- **AI 能力**：预留可配置的 AI 服务适配层
  - 文本评审/反馈：大模型 API（国内大模型或 OpenAI 兼容接口）
  - 语音转写：ASR 服务（国内云厂商 ASR 或 Whisper API）
  - PPT 内容提取：pdf 解析 + pptx 文本提取，必要时调用 OCR
- **部署**：单台云服务器 + Docker Compose（nginx + 应用），HTTPS

## 5. 部署需求

- 单台服务器：2 核 4G 起，建议 4 核 8G；磁盘 100G（20 人 × 10 分钟音频 + PPT + 备份）
- 系统：Ubuntu 22.04 / CentOS 均可
- 需要域名（用于 HTTPS 证书）；若无域名可先用 IP + 自签证书
- 腾讯云 / 阿里云均可，建议机房选团队所在地附近（如上海）
- 国内服务器直连 OpenAI 不稳定，需要确认使用的 AI 服务商（见待确认项）

## 6. 待确认项

1. **AI 服务商**：国内大模型（通义千问 / 腾讯混元 / 豆包）还是 OpenAI（需代理）？语音转写用同一家云厂商的 ASR 还是 Whisper API？
2. **部署目标**：腾讯云还是阿里云？是否有域名？
3. **时间线**：Workshop 日期？PPT 截止上传日期？用于确定开发排期与 MVP 范围
4. **品牌与界面**：是否需要平台名称、Logo、配色（默认简洁中性的工作台风格）
5. **数据隐私**：演讲录音是否允许留档（仅平台内可见，还是可导出）？

## 7. 后续里程碑（草案）

- **M1 基础平台**：登录、管理员建号、PPT 上传、AI 评审意见
- **M2 汇报闭环**：录音、转写、AI 反馈、进度看板
- **M3 打磨**：历史记忆增强点评、移动端适配、备份/导出

## 8. 实现状态（2026-08-05）

**已完成（MVP）**

- 登录 / 退出、管理员建号、重置密码、停用/启用账号
- 新建汇报、上传 PPT（.pptx/.pdf）、自动提取文字（含演讲者备注）
- AI 评审：按固定维度（业务影响力 / 创新性 / 落地完成度 / 问题与解决 / 数据支撑 / 下一步 / 表达结构）逐项输出修改意见与优先级；评审维度可由管理员配置
- 演讲录音（浏览器麦克风）+ 上传 + 火山引擎 BigModel ASR 转写（极速版，标准版可切换，支持手动转写兜底）
- AI 反馈：综合最终 PPT + 演讲转写 + 个人历史汇报生成评论（亮点 / 改进建议 / 历史对比）
- 管理员进度看板：PPT / 评审 / 录音 / 转写 / 反馈 五步完成情况
- 部署：Docker + docker-compose + nginx（含 ffmpeg 音频转码），境外服务器

**待配置（交付前）**

- 火山方舟 `ARK_API_KEY`（豆包大模型）与模型 ID
- 火山语音 `VOLC_SPEECH_API_KEY`（大模型 ASR）并在控制台开通「录音文件识别」能力
- 域名与 HTTPS
- 20 位同事账号名单（姓名 + 邮箱 + 初始密码）
- 确认汇报人是否需要"PPT 定稿"环节（当前评审后可反复上传）
